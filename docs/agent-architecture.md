# Архитектура ИИ-агента

Агент ведёт переписку с новым клиентом от лица энергопрактика: отвечает как
человек, поддерживает разговор и шаг за шагом доводит до сообщения с ценами.
После цен чат помечается «у менеджера», дальше отвечает менеджер.

Документ фиксирует решения владельца от 24–25.09.2026 и заменяет прежние
версии. `new-ai-agent.md` описывает другую модель (ранняя передача
менеджеру, воронка после цен) и используется только как источник идей для
библиотеки фраз.

## 0. Три принципа

1. **Код решает «когда и что отправить», LLM решает «как сказать».**
   Порядок вех воронки, тайминги, тексты диагностик, предложений и цен —
   в коде и библиотеке. Формулировки, реакция на слова клиента, связки —
   у LLM. Прошлый агент перескакивал этапы и слал не те шаблоны именно
   потому, что выбирал и то и другое сам.
2. **Сначала разговор, потом воронка.** В каждом ходе агент сперва отвечает
   на всё, что клиент спросил и рассказал, и только потом делает не больше
   одного шага к следующей вехе. Если клиент хочет поговорить, шаг
   откладывается.
3. **Ничего не выдумывать.** Цены, ссылки, содержание диагностик, факты о
   практике и о клиенте берутся только из библиотеки, образа и памяти.
   Что проверяемо кодом, проверяется кодом; остальное — отдельным
   проверяющим промптом.

## 1. Решения владельца

| Тема | Решение |
|---|---|
| Граница | До цен агент ведёт диалог сам, включая сомнения и вопросы о практиках. После отправки цен чат уходит менеджеру. Напоминаний после цен нет. |
| Передача менеджеру | Агрессия, кризис, просьба позвать человека, вопрос «вы бот?» (не отрицать и не уклоняться), непонятные сообщения, любое медиа от клиента, язык без материалов в библиотеке. |
| Менеджер | Пишет из веба или из Telegram на телефоне. Любое исходящее не от агента переводит чат в режим менеджера. Возврат к агенту — вручную в вебе. |
| Включение | На аккаунте и отдельно в каждом чате. Только диалоги, начатые клиентом после включения на аккаунте. Старые чаты не трогать. |
| Диагностика | Дословно из библиотеки по категории × пол × язык. LLM добавляет 1–2 предложения до и после, смысл не меняет. |
| Данные клиента | Пол, город, дату рождения, категорию и язык извлекает отдельный промпт с температурой 0. Пол по имени и тексту, при сомнении универсальная диагностика. |
| Нет данных рождения | Одно напоминание, затем универсальная диагностика по расписанию. |
| Цена раньше времени | Мягко отложить, вести дальше. Цены только после предложения. |
| Молчание | Не больше 3 напоминаний. Следующее сообщение только после прочтения предыдущего; сутки не прочитано — одно напоминание и стоп. |
| Очередь сообщений | Несколько сообщений подряд — один ход, ответ на каждое. Ответ пересобирается, если клиент дописал. |
| Режим работы | Круглосуточно, ночных окон и ночных задержек нет. |
| Язык | Определяет LLM, агент отвечает на языке клиента. Нет материалов на языке — менеджер. |
| Модель | DeepSeek `deepseek-chat`. |
| Образ и тексты | Образ практика, библиотека, цены — свои у каждого аккаунта, редактируются в вебе. |
| Не делаем | Медиа от агента, черновики и обучение на правках менеджера, линию «наставничество», обработку правок и удалений сообщений клиентом. |
| Песочница | Обязательна: симуляция в вебе без Telegram, с виртуальным временем, в том числе из копии реального чата. |

## 2. Модель: разговор и воронка

Агент — это два слоя, которые работают одновременно.

**Воронка** — четыре вехи, которые нужно доставить клиенту по порядку. Веха
= длинный текст из библиотеки, который код отправляет, когда выполнены
предпосылки и сработал триггер. LLM веху не выбирает и не пишет, только
обрамляет.

**Разговор** — всё остальное: ответы на вопросы, реакция на рассказ,
эмпатия, вопросы клиенту. Это работа LLM. Внутри разговора агент делает
**подталкивания** — по одному за ход — которые готовят следующую веху.

### 2.1 Вехи

| Веха | Что уходит | Предпосылки | Не раньше | Триггер |
|---|---|---|---|---|
| `links` | ссылки на страницы + «займусь диагностикой, вернусь» | клиент рассказал, что беспокоит, **или** прошло 2 хода без запроса | сразу | в том же ходе, где выполнились предпосылки |
| `diagnostic` | диагностика по категории × пол × язык + обрамление | `links` отправлены; дата и место есть **или** ушло напоминание о данных | 45–75 мин после `links` (случайно) | таймер; **или** клиент спросил «ну что там?» после минимума — в том же ходе |
| `offer` | описание практик + обрамление | `diagnostic` прочитана | сразу после прочтения | клиент содержательно откликнулся и (интерес ≥ 2 или спросил, что делать / о практиках); **или** лестница молчания |
| `prices` | стоимость + обрамление | `offer` прочитано | сразу после прочтения | клиент спросил цену или выразил готовность; **или** лестница молчания |

После `prices` — режим менеджера, ярлык «цены отправлены, молчит» до
первого ответа клиента.

Предпосылки и «не раньше» проверяются кодом из базы: `read_at` исходящих,
время отправки прошлой вехи, карточка клиента. Триггеры бывают двух видов:
намерение клиента из анализа (в ходе клиента) или срабатывание лестницы
молчания (ход по расписанию).

Обрамление вехи — 1–2 предложения до и после по памяти о клиенте — пишет
LLM в том же ходе; тело вехи вставляет код и сверяет побайтно.

### 2.2 Этап

Этап не хранится отдельно, он равен последней доставленной вехе:
`intake` (ничего) → `links` → `diagnostic` → `offer` → `prices`. Всё, что
раньше называлось «разрешёнными действиями» и «условиями выхода», сведено
в таблицу вех. Код не даёт отправить веху, если предыдущая не доставлена.

### 2.3 Подталкивания

Подталкивание — вопрос или приглашение, которое готовит следующую веху.
Не больше одного за ход, только после ответов клиенту, вплетённое в
разговор.

| Этап | Чего не хватает | Подталкивание |
|---|---|---|
| `intake` | даты и места | попросить дату и место рождения (один раз, потом одно напоминание) |
| `intake` | запроса | спросить, что беспокоит / на какую сферу смотреть (не больше 2 ходов) |
| `intake` | одного очевидного факта | уточнить его (например, есть ли сейчас пара) — только один вопрос |
| `links` | ничего | поддерживать ожидание, разговаривать; диагностику не пересказывать |
| `diagnostic` | отклика | спросить, что откликнулось / что хотел бы изменить |
| `offer` | вопросов или готовности | всё ли понятно, какая практика ближе, есть ли вопросы |

Правила подталкиваний:

- **Сначала клиент.** Все пункты для ответа закрываются раньше подталкивания.
- **Одно за ход.** Нельзя спросить дату, место и сферу и прислать ссылки
  в одном ходе.
- **Вплетено.** Вопрос привязан к тому, что клиент только что сказал.
  Плохо: «А вы в каком городе живёте?» → «Скажите ваш город». Хорошо:
  «Сейчас в Шамони, горы и тишина 🙂 А вы откуда? Заодно напишите дату
  рождения, посмотрю вашу карту».
- **Право поговорить.** Если клиент увлечён (делится историей, задаёт
  вопросы подряд), подталкивание пропускается. Счётчик
  `turns_without_nudge` растёт; на третьем ходе подряд план хода требует
  мягко вернуть к шагу.
- **Отложенный ответ вместо отказа.** На ранний вопрос о цене агент
  говорит, когда к этому вернётся, и продолжает разговор.
- **О себе только из образа.** Вопросы о практике и биографии — по образу
  и разделу «о себе и о работе» библиотеки. Чего там нет, агент не
  выдумывает: отвечает общо и переводит фокус на клиента.

### 2.4 Лестница молчания

Если клиент прочитал последнее сообщение агента и молчит, воронка идёт
дальше по расписанию. Каждая ступень — ход по расписанию (раздел 3), с
живым текстом по памяти, а не шаблоном.

| Этап | Ступень | Через | Что происходит |
|---|---|---|---|
| `intake` | напоминание о данных | 60–90 мин после просьбы о данных | подталкивание; после него предпосылка `diagnostic` считается выполненной |
| `diagnostic` | вопрос-отклик | 45–75 мин после прочтения | подталкивание (напоминание 1) |
| `diagnostic` | предложение | 12–16 ч | веха `offer` |
| `offer` | вопрос | 12–16 ч | подталкивание (напоминание 2) |
| `offer` | цены | 12–16 ч | веха `prices` → менеджер |
| любой | непрочитанное | 24 ч без прочтения | одно напоминание (напоминание 3), дальше ступени не ставятся |

Интервалы случайные внутри диапазона. Ступень ставится только когда
предыдущее сообщение агента прочитано; иначе ждёт ступень «непрочитанное».
Напоминаний не больше трёх на чат; вехи в лимит не входят. После лимита
чат остаётся в `auto`: если клиент напишет, разговор продолжается с
текущего этапа.

Ответ клиента отменяет ступени, до которых больше 10 минут, и они ставятся
заново после хода. Ступень, которая созрела или созреет в ближайшие
10 минут, входит в ход клиента как «срочное»: ответ получается один и
связный («как раз закончил смотреть, сейчас пришлю»).

**Как это устроено.** Ступень не «ставится и снимается» по событиям, а
вычисляется: `nextLadderStep` выводит одну следующую ступень из этапа,
реестра сказанного, прочтения последнего сообщения и счётчика напоминаний.
После каждого хода и каждого «прочитано» ожидающие ступени чата
заменяются результатом. Интервал внутри диапазона выбирается
детерминированно от чата и точки отсчёта, поэтому пересчёт без изменений
ничего не сдвигает. Условие ступени ещё раз проверяет план хода в момент
срабатывания: устаревшая закрывается без обращения к модели. Таймер
диагностики после напоминания о данных отсчитывается от напоминания.

Ходы по расписанию пишутся коротко (не больше двух сообщений): агент пишет
первым, поэтому не благодарит и не отвечает, не утешает повторно, не
повторяет своих прошлых фраз; во вступлении к вехе нет вопросов — вопрос
после неё. Проверяющий ловит ответ «в пустоту» кодом `answers_nothing`.

## 3. Ход

Ход — единица работы агента. Всё, что агент отправляет, проходит один
конвейер, независимо от того, что его запустило.

### 3.1 Запуск

- **Ход клиента**: входящее сообщение в чате с `mode = auto`.
- **Ход по расписанию**: созрела ступень лестницы молчания или таймер
  вехи `diagnostic`.

Чат впервые попадает к агенту, когда ingest создаёт диалог, начатый
клиентом, на аккаунте с включённым агентом, и время первого сообщения
позже времени включения. Тогда создаётся `bot_chat_state` с `mode = auto`.
Остальные чаты агент не видит, пока их не передали руками. Переключатель
аккаунта означает «брать новые диалоги»: чат, переданный агенту кнопкой в
переписке, агент ведёт и при выключенном переключателе (так тестовый чат
не тянет за собой настоящих клиентов).

### 3.2 Сборка хода клиента

На чат держится окно тишины: входящее запускает или продлевает его на
25–45 с (случайно), событие «печатает» продлевает ещё на 15 с, верхняя
граница 3 мин. Когда окно закрылось, ход = все входящие с момента
последнего сообщения агента, по порядку. Сообщение, пришедшее после
закрытия окна, но до начала генерации, включается в ход.

У чата есть `generation_seq`. Входящее во время генерации увеличивает его;
результат со старым номером выбрасывается, сборка начинается заново. Если
входящее пришло во время доставки многочастного ответа, оставшиеся части
не отправляются, а следующий ход видит в истории то, что уже ушло.

На чат держится **замок**: ход клиента и ход по расписанию не выполняются
одновременно. Ход по расписанию, заставший замок, ждёт и перепроверяет
своё условие после освобождения.

### 3.3 Анализ

Промпт-анализатор, температура 0, JSON. Вход: карточка, активные факты,
резюме, последние 10 сообщений, новый ход. Выход:

| Поле | Содержание |
|---|---|
| `card` | обновления карточки с уверенностью 0–1 по полю |
| `facts` | новые факты и противоречия с активными |
| `summary` | обновлённое резюме |
| `language` | язык новых сообщений: код из списка, `other` или `null`, если по ним не понять («ок», эмодзи, дата) |
| `risk` | агрессия, кризис, просьба позвать человека, вопрос «вы бот», непонятно |
| `intents` | несколько из: `asks_price`, `asks_practice`, `asks_about_practitioner`, `asks_diagnostic_status`, `shares_story`, `ready`, `doubts`, `objects`, `smalltalk`, `answers_question`, `silent_ack` |
| `objection` | категория возражения из списка плейбука или `null` |
| `interest` | 0–3 |
| `mood` | спокоен, грустит, тревожится, скептичен, раздражён |
| `answer_points` | что в ходе требует ответа (вопрос, факт, просьба, эмоция) и что можно пропустить («ок», стикер); один пункт может покрывать несколько сообщений; у пункта есть тема: `price`, `practice`, `diagnostic`, `practitioner`, `client`, `other` |

**Код не разбирает текст по смыслу.** Всё, по чему принимаются решения
(тема вопроса, язык, намерение, возражение, риск), анализатор называет
значением из закрытого списка; незнакомое значение отбрасывается, а не
угадывается. Регулярки в коде допустимы только для чисто синтаксических
проверок (сумма с валютой, адрес, письменность) — раздел 3.7.

**Язык «липкий».** Первый определённый язык записывается в карточку сразу;
сменить его может только ход, где клиент написал хотя бы 20 букв. «ok 👍»
не переключает диалог на английский и не уводит его к менеджеру. План и
ответчик берут язык из карточки.

Для хода по расписанию анализ пропускается: намерений нет, память
актуальна.

### 3.4 План хода

План собирает код, без LLM. Это главное место, где живёт «умность» в
управляемом виде. План состоит из пяти частей в фиксированном порядке:

1. **Передача.** Если `risk`, медиа, нет материалов на языке — ход
   завершается передачей менеджеру (раздел 5). Дальше не идёт.
2. **Ответить.** `answer_points` из анализа, в порядке клиента. Темы,
   которые раскрываются только вехой (`TOPIC_HOLDS`: цена — вехой
   «стоимость», результаты диагностики и подробности практик — вехой
   «диагностика»), получают формулу ответа без раскрытия: «к стоимости
   вернусь чуть позже». Если веха этого хода сама закрывает тему (веха
   «ссылки» и есть «займусь диагностикой»), формула не нужна. Какие темы
   раскрывать, решает веха хода, поэтому ответы собираются после неё.
3. **Веха.** Если предпосылки, «не раньше» и триггер какой-то вехи
   выполнены (включая срочные ступени), веха входит в план: LLM пишет
   обрамление, код вставляет тело.
4. **Подталкивание.** Если вехи в ходе нет, план выбирает одно
   подталкивание по таблице 2.3 или «пропустить» (клиент увлечён и
   `turns_without_nudge < 2`). При возражении план указывает подход из
   плейбука: первый или, при повторе категории, следующий.
5. **Ограничения.** Список «не повторять» из реестра сказанного, язык,
   лимит частей, запрет упоминать цены и содержание непройденных вех.

Пример плана:

> Ответить: 1) спросил, где ты живёшь — по образу, коротко и живо;
> 2) рассказал, что недавно развёлся — отреагируй по существу, без
> дежурных фраз. Веха: нет. Подталкивание: спроси его город и дату
> рождения, привязав к разговору о городах. Не повторять: вопрос о сфере.
> Не упоминать: цены, содержание диагностики.

Правила плана лежат в `turn/plan.ts` и покрыты таблицами тестов
«состояние + анализ → план». Это единственное место, которое нужно
править, когда меняется логика воронки.

### 3.5 Текст

Промпт-ответчик, температура ~0.7, JSON-режим провайдера — ответ всегда
разбирается, служебное не может утечь клиенту:

```
{ "messages": ["Понимаю вас, это правда тяжело 🙏",
               "Смотрите, чистка как раз с такими блоками и работает…"],
  "after_block": [],
  "nudge": "ask_goal", "arguments": ["cleanse_explains_blocks"],
  "unanswered_about": [], "notes": "ответил про чистку, увёл к цели" }
```

`messages` — сообщения по порядку; `after_block` — короткое продолжение
после тела вехи (вопрос-отклик), без вехи пусто. Место вехи задаёт
структура, а не маркер в тексте: код ставит тело между `messages` и
`after_block`. `nudge` — какое подталкивание сделано или `null`;
`arguments` — какие подходы из плейбука использованы; `unanswered_about` —
вопросы о практике, на которые пришлось ответить общо; `notes` — только в
журнал.

### 3.6 Проверка

Промпт-проверяющий, температура 0, JSON. Получает план, память, ход
клиента и черновик. Возвращает список нарушений по чек-листу:

- каждый пункт «ответить» закрыт, в порядке клиента, раньше подталкивания;
- подталкиваний не больше одного, оно связано с разговором;
- нет фактов о клиенте, которых нет в памяти; нет фактов о практике,
  которых нет в образе и разделе «о себе»;
- нет обещаний сроков, нет цен, сумм и ссылок, кроме библиотечных;
- ничего из «не повторять»; ничего из «не упоминать»;
- язык, обращение на «вы», длина и число частей в норме;
- в конце вопрос или приглашение, если план его требует;
- текст не выходит за план (не продаёт, когда план «поддержать ожидание»).

Ответчик вызывается второй раз с замечаниями только при грубых
нарушениях (`HARD_CODES`); стиль идёт в журнал и лишнего вызова не стоит.
Повторная проверка — только если первая нашла выдуманные факты, цены,
повторы, запрещённые темы или не тот язык (`BLOCKING_CODES`), и она
отмечает только грубое. Если такое осталось, текст не уходит: веха (если
есть) отправляется сама, без вехи — короткая нейтральная фраза из
библиотеки; ход помечается в журнале. Обычный ход — три вызова модели,
с переписыванием — четыре-пять.

### 3.7 Жёсткие проверки

Код, без LLM, последняя линия:

- тело вехи берётся из библиотеки побайтно и встаёт между `messages` и
  `after_block`;
- в тексте ответчика нет ни одной суммы с валютой (цены уходят только
  телом вехи «стоимость»), все адреса — из образа и библиотеки, нет
  служебной разметки (`{{`, ключей JSON); такая часть режется;
- письменность совпадает с языком карточки — только явный промах на
  длинном тексте (русский ответ латиницей), адреса не считаются;
- число частей в лимите, одно место держится под вопрос после вехи;
  каждая часть влезает в 4096 символов;
- отправлять нечего — короткая фраза из библиотеки.

### 3.8 Доставка

Агент ведёт себя как человек с телефоном в руке:

- **Присутствие.** После нашего сообщения практик «в чате» около 5 мин и
  отвечает через 10–40 с. Клиент написал спустя 5–60 мин — 1–4 мин.
  Спустя больше часа — 3–12 мин. Новый лид — 60–240 с. Ходы по расписанию
  без задержки: их момент и так случайный.
- **«Прочитано»** ставится один раз, когда окно тишины закрылось, перед
  «печатает».
- **«Печатает»** ~6 символов в секунду, не более 25 с на часть. Веха:
  «печатает» 5–10 с, потом один текст — как вставка из заметок.
- **Части** уходят с паузами 3–10 с, по одной-две темы на часть.
- Все задержки с джиттером, ночью те же.

Перед первой отправкой ход получает ключ идемпотентности
`chat_id + generation_seq` и записывает его в журнал; после перезапуска
ход с известным ключом повторно не отправляется. `telegram_message_id`
каждого отправленного сообщения сохраняется — это список «своих» для
страховки от чужих исходящих.

### 3.9 Закрытие хода

- память: карточка, факты, резюме из анализа; реестр сказанного —
  доставленная веха, подталкивание, аргументы, ссылки;
- счётчики: `turns_without_nudge`, `reminders_sent`;
- ступени лестницы молчания ставятся заново от текущего этапа;
- журнал получает ход целиком.

## 4. Память о клиенте

Единый объект `Memory`, который анализ обновляет, а план и ответчик читают.

| Слой | Где | Что | Кто пишет |
|---|---|---|---|
| Карточка | `bot_chat_state.card` | имя, пол, дата и место рождения, категория, язык; уверенность и id сообщения-источника по полю | анализатор |
| Факты | `bot_client_facts` | свободные утверждения: `situation`, `emotion`, `objection`, `biography`, `preference`, `expectation`; источник, уверенность, `active`/`superseded` | анализатор |
| Резюме | `bot_chat_state.summary` | 5–8 предложений: с чем пришёл, что произошло, что обещано, где разговор | анализатор |
| Сказанное | `bot_chat_said` | доставленные вехи, заданные подталкивания, использованные аргументы, ссылки; id сообщения и время | код по результату хода |

Пороги: пол с уверенностью ниже 0.8 и категория ниже 0.7 считаются
неизвестными и дают универсальную диагностику. В промпт идут до 30
активных фактов, свежие первыми. Повторное возражение одной категории
видно по фактам и меняет подход в плане. Что не хранится: сырые сообщения
(они в `telegram_messages`), выводы LLM о характере клиента, всё, что не
подтверждено его словами.

## 5. Передача менеджеру

Единый список причин и кто их обнаруживает:

| Причина | Обнаруживает | Ярлык в вебе |
|---|---|---|
| медиа от клиента | код, по `media_kind` входящего | нужен ответ |
| агрессия, кризис, «позовите человека», «вы бот?», непонятно | анализатор (`risk`) | нужен ответ |
| нет материалов на языке клиента | код, при выборе вехи | нужен ответ |
| ответ клиента после `prices` | код | нужен ответ |
| отправлены `prices`, клиент молчит | код | цены отправлены, молчит |
| чужое исходящее в чате | код, по списку «своих» id (гонка с `sendText` закрывается сверкой текста с очередью ожидающих отправок) | без ярлыка: менеджер уже в чате |
| LLM или Telegram недоступны дольше 30 мин | код | агент недоступен |

При передаче: `mode = manager`, ступени отменяются, в состоянии
сохраняются причина и время. Агент в чате больше не пишет. Любое сообщение
клиента в чате у менеджера ставит ярлык «нужен ответ» (в том числе после
«цены отправлены, молчит»); ответ менеджера — из веба или с телефона —
ярлык снимает; чат остаётся в `manager`.

Режимы чата: `auto`, `manager`, `off`. Переключатели в вебе: на аккаунте
(агент для новых диалогов) и в чате (`auto`/`off`; возврат из `manager` в
`auto` вручную, ступени ставятся заново от текущего этапа). Отправка из
веба — такое же чужое исходящее, как с телефона: чат уходит менеджеру.

## 6. Образ и библиотека

Всё по аккаунту, редактируется в вебе.

**Образ** (`bot_account_settings.persona`): имя, пол, биография (откуда,
где живёт, как пришёл к практике), ссылки на страницы.

**Библиотека** (`bot_library_items`: `kind`, `language`, `gender`,
`category`, `title`, `text`, `sort`):

| Вид | Роль |
|---|---|
| `greeting`, `ask_birth_data`, `no_birth_data`, `ask_request`, `empathy`, `wait`, `return_question`, `nudge`, `price_deflect` | образцы тона для разговора и подталкиваний; ответчик их пересказывает, не цитирует |
| `links`, `diagnostic`, `offer`, `prices` | тела вех, уходят дословно |
| `objection` | плейбук возражений: категория `expensive`, `think_about_it`, `dont_believe`, `no_time`, `ask_partner`, `tried_before`, `later`; 2–3 подхода на категорию с порядком при повторе |
| `about` | «о себе и о работе»: вопрос и ответ от лица практика — где живёт, сколько практикует, как проходит чистка, что чувствуют после, чем отличается от психолога; единственный источник фактов о практике |

Выбор диагностики: категория × пол × язык; нет — универсальная для пола и
языка; нет и её — передача менеджеру.

**Примеры** (`bot_examples`: `stage`, `situation`, `client`,
`practitioner`, `enabled`): отобранные вручную обмены из реальной
переписки, 3–5 на этап, добавляются кнопкой «в примеры» в чате с заменой
имён, дат и городов на обобщённые. Идут в промпт ответчика как эталон тона
и длины.

**Тайминги** (`bot_account_settings.timings`): окно тишины, задержки
присутствия, скорость «печатает», интервал `diagnostic`, интервалы
ступеней, лимит напоминаний, `max_turns_without_nudge`.

Импорт из `docs/source/*.xlsx` — разовый скрипт при создании аккаунта;
лист «наставничество» и блоки с видео не импортируются.

## 7. Сборка промптов

Порядок частей одинаков во всех промптах и подчинён кэшу префикса
DeepSeek: **статика → история → память → план → новый ход**. Статика
(образ, правила, примеры и библиотека этапа) и старый хвост истории
совпадают с прошлым вызовом и оплачиваются по кэш-тарифу; память и план
меняются каждый ход и стоят в конце, рядом с ходом клиента, где модель
читает внимательнее.

История идёт целиком, до 60 сообщений; старшие обрезаются и остаются
только в резюме. Тела вех в истории заменяются заглушкой
`[отправлена диагностика: отношения, женская]`, медиа клиента —
`[фото]`, `[голосовое]`.

Системные правила поведения общие для всех аккаунтов и живут в коде;
всё, что отличает практика, — в образе и библиотеке.

## 8. Каналы, время, песочница

Ядро не знает, откуда пришли события и куда уходят сообщения:

```ts
interface Inbox   { subscribe(handler: (e: InboxEvent) => void): () => void }
// InboxEvent: message | read | typing — с chatId и телом
interface Channel { send(chat, text): Promise<{ messageId }>; setTyping(chat, on); markRead(chat) }
interface Clock   { now(): Date; sleep(ms): Promise<void> }
```

Telegram: `Inbox` над `TelegramEventsService.subscribe`, `Channel` над
`TelegramOutboundService`, реальные часы. Планировщик (`bot_jobs`,
поллер раз в 30 с) и доставка берут время только из `Clock`.

**Песочница**: `Inbox` — форма в вебе, где владелец пишет за клиента и
жмёт «прочитано»; `Channel` пишет в `bot_sandbox_messages`; `Clock`
виртуальный, кнопка «промотать до следующего события» двигает его к
ближайшей ступени и выполняет её. Задержки доставки показываются числом,
не ждутся. Состояние, память и журнал — те же таблицы, что у боевых чатов,
с `sandbox = true`.

**Из реального чата**: у сообщения в переписке кнопка «продолжить в
песочнице». Копируется история до этого сообщения включительно, память
восстанавливается прогоном анализатора по копии, этап — по последней
доставленной вехе. В исходный чат ничего не пишется.

## 9. Данные

| Таблица | Назначение |
|---|---|
| `bot_account_settings` | включён ли агент, `enabled_at`, образ, тайминги, модель |
| `bot_library_items` | библиотека аккаунта (раздел 6) |
| `bot_examples` | примеры реальных диалогов |
| `bot_chat_state` | `mode`, карточка, резюме, `turns_without_nudge`, `reminders_sent`, `last_handled_message_id`, `generation_seq`, причина и время передачи, ярлык, `sandbox` |
| `bot_client_facts` | факты о клиенте |
| `bot_chat_said` | реестр сказанного |
| `bot_jobs` | ступени и таймеры: `chat_id`, `kind`, `run_at`, `status` |
| `bot_turns` | журнал ходов: вход, анализ, план, черновик, замечания проверяющего, вторая версия, жёсткие проверки, отправленное с задержками, ключ идемпотентности, ошибки |
| `bot_prompt_snapshots` | полные промпты и ответы LLM, чистятся через 30 дней |
| `bot_sandbox_sessions`, `bot_sandbox_messages` | песочница |

Этап не хранится: вычисляется по `bot_chat_said` (последняя веха).

## 10. Надёжность

- Сбой LLM внутри хода: таймаут вызова 30 с, два повтора с паузой 2 и
  5 с; ответ, который не разобрался как JSON, запрашивается ещё раз. Ход,
  который всё равно не удался, повторяется заданием с нарастающей паузой
  (1, 2, 5, 10, 12 мин) — для человека такая задержка естественна; после
  30 минут сбоев чат уходит менеджеру с ярлыком «агент недоступен».
- Поллер забирает задания через `FOR UPDATE SKIP LOCKED`, ход по заданию
  защищён ключом `job:<id>` — два экземпляра API одно задание не выполнят.
  Задания, зависшие в `running` после падения, через 15 минут
  возвращаются в очередь.
- Перезапуск API: ходы с записанным ключом идемпотентности не
  отправляются повторно; ступени в `bot_jobs` переживают перезапуск;
  окна тишины пересобираются из `last_handled_message_id`.
- Второй инстанс API рядом с боевым запускать только без Telegram
  (иначе Telegram отзывает сессию); ядро при этом работает в песочнице.
- Правки и удаления сообщений клиентом не обрабатываются: ход видит текст
  на момент получения.

## 11. Наблюдаемость

**Журнал** (`bot_turns`) отвечает на «почему агент так сказал» по любому
ходу: анализ, план, черновик, замечания проверяющего, что ушло. В чате
журнал раскрывается под каждым сообщением агента.

## 12. Веб

- страница аккаунта: вкладка «Агент» с переключателем, образом,
  таймингами;
- редактор библиотеки по видам, языкам, полу и категориям, включая
  плейбук возражений и раздел «о себе»;
- редактор примеров, кнопка «в примеры» в чате;
- в чате: режим, ярлык и причина передачи, журнал под сообщениями агента,
  кнопка «продолжить в песочнице»;
- список «у менеджера»: «нужен ответ» и «агент недоступен» сверху с
  временем ожидания, «цены отправлены, молчит» ниже;
- песочница: диалог, виртуальные часы, журнал.

## 13. План работ

1. **Каркас.** Модуль `apps/api/src/bot/`, таблицы и миграции, импорт
   библиотеки из xlsx, настройки аккаунта, переключатели, REST.
   *Сделано 25.09.2026:* миграция `CreateBotTables`, справочники в
   `library/kinds.ts`, образ и тайминги с умолчаниями, стандартная
   библиотека из таблиц (`library/seed`, скрипт `scripts/build-library-seed.mjs`),
   REST для настроек, библиотеки, примеров и режима чата
   (`apps/api/README.md`, раздел «Агент»), вкладка «Агент» в вебе:
   включение, импорт библиотеки, образ, основные тайминги. Таблицы
   песочницы, переключатель режима в чате и редакторы библиотеки и
   примеров в вебе — в этапах 3 и 6.
2. **Ядро.** Сборка хода, анализ, план хода с таблицами тестов, ответчик,
   проверяющий, жёсткие проверки, память, реестр сказанного, доставка,
   журнал. Через `Inbox`/`Channel`/`Clock`.
   *Сделано 25.09.2026:* `apps/api/src/bot/core` (чистые модули, 64
   теста), три промпта в `prompts/`, клиент DeepSeek, репозитории памяти,
   журнала и заданий, `TurnRunnerService`. Живой прогон через канал в
   памяти прошёл всю воронку: знакомство → ожидание → диагностика по
   расписанию → предложение → цены → менеджер. Уточнения по итогам прогона:
   блокируют отправку только выдуманные факты, цены, повторы, запрещённые
   темы и язык (остальные замечания проверяющего — повод переписать);
   при пустых ссылках в образе веха `links` уходит как сообщение об
   ожидании; поколение и замок чата — в исполнителе, поллер заданий —
   этап 4.
3. **Песочница.** Веб-форма, виртуальные часы, запуск из реального чата.
   Обкатка воронки целиком до выхода в Telegram.
   *Сделано 25.09.2026:* таблицы `bot_sandbox_sessions` и
   `bot_sandbox_messages`, `BotSandboxService` (виртуальные часы, ходы и
   перемотка в фоне, «клиент дописал» во время хода), REST
   (`apps/api/README.md`, раздел «Песочница»), вкладка «Песочница» в вебе:
   сессии, диалог с задержками, «клиент прочитал», перемотка до события и
   на +10 мин/+1 ч/+1 день, журнал ходов (анализ, план, проверяющий,
   вырезанное), память и задания. «Продолжить в песочнице» — кнопкой в
   шапке чата и иконкой у сообщения; этап копии — по вехам из библиотеки,
   память — ходом `restore`. По итогам обкатки: условие задания
   проверяется при срабатывании (устаревшее — без модели), «ходов на этапе»
   считаются по истории, ответчик и проверяющий видят начало тела вехи и
   не повторяют его смысл во вступлении.
4. **Лестница молчания.** `bot_jobs`, ступени, условия прочтения, лимиты.
   *Сделано 25.09.2026:* `core/ladder.ts` (ступень из состояния, таблица
   тестов), пересчёт после хода и «прочитано» (`BotLadderService`), поллер
   (`BotSchedulerService`, стартует с регистрацией канала на этапе 5),
   общий исполнитель заданий для поллера и песочницы, повторы после сбоя
   до 30 минут. План хода задания больше не ставит. В песочнице вся
   лестница проходит сама: ссылки → диагностика по таймеру → вопрос-отклик
   → предложение → вопрос → цены → менеджер.
5. **Telegram.** `Inbox`/`Channel` над Telegram-модулем, первый контакт с
   чатом, страховка от чужих исходящих, режимы. Обкатка на тестовом чате
   Denis.
   *Сделано 25.09.2026:* `BotTelegramService` — входящие через сборщик
   хода (окно тишины, «печатает»), ход берёт все неотвеченные из базы,
   новые диалоги по включённому аккаунту, чужое исходящее → менеджер
   (`core/telegram-inbox.ts`: свои id и тексты в отправке), «прочитано» →
   пересчёт лестницы, после переподключения аккаунта неотвеченное —
   заданием `reply`; канал для поллера. Живая обкатка на чате Denis — с
   владельцем (нужно писать с аккаунта Denis).
6. **Веб.** Журнал в чате, список «у менеджера», редакторы.
   *Сделано 25.09.2026:* кнопка «Агент» в шапке чата и панель (режим,
   передать/вернуть агенту, журнал ходов, память, задания), пометка
   «ответ агента» у сообщений с переходом к ходу, «В примеры» у исходящих,
   вкладка «У менеджера» (ждущие ответа сверху), во вкладке «Агент» —
   подвкладки «Библиотека» и «Примеры» с редакторами.
